PART 7 — Replit Implementation Guide (Full Markdown, Clean Version)
이 섹션은 Replit 상에서 Collaboom MVP를 실제로 구현하기 위한 구체적인 개발 가이드다.
 여기까지 정의된 PRD(Part 1~6)를 바탕으로, Replit + Supabase + React + TypeScript + Tailwind + shadcn/ui 조합으로 프로젝트를 세팅하는 기준을 제공한다.

1. Tech Stack
Frontend


React 18


TypeScript


Vite (또는 CRA / Next.js 중 하나, 여기서는 Vite 기준)


react-router-dom


Tailwind CSS


shadcn/ui


Backend / Data


Supabase (Auth + Postgres DB)


Supabase JS Client


Infra


Replit (Node.js + React + TS 템플릿)


Google OAuth (Supabase Auth Provider)


Gmail / Google Apps Script (Notification 연동은 별도 시스템, 이 PRD에서는 API Hook만 고려)



2. 프로젝트 생성 및 기본 설정
Replit에서 “Node.js + React + TypeScript” 템플릿 선택 또는 Vite + React + TS 초기화


Tailwind CSS 설치 및 설정


shadcn/ui 설치


Supabase client 설정



3. 폴더 구조
권장 폴더 구조:
src/
  main.tsx
  App.tsx

  routes/
    influencer/
      LandingPage.tsx
      CampaignListPage.tsx
      CampaignDetailPage.tsx
      InfluencerDashboardPage.tsx
      ProfilePage.tsx

    admin/
      AdminDashboardPage.tsx
      AdminCampaignListPage.tsx
      AdminCampaignDetailPage.tsx
      AdminApplicantsTab.tsx
      AdminShippingTab.tsx
      AdminUploadsTab.tsx
      AdminNotesTab.tsx

  components/
    layout/
      MainLayout.tsx
      InfluencerLayout.tsx
      AdminLayout.tsx
    ui/
      CampaignCard.tsx
      Badge.tsx
      Button.tsx
      Tabs.tsx
      Table.tsx
      Modal.tsx
      Toast.tsx

  lib/
    supabaseClient.ts
    auth.ts            // auth helper (influencer/admin 구분)

  hooks/
    useAuth.ts
    useInfluencerProfile.ts
    useCampaigns.ts
    useAdminCampaigns.ts

  styles/
    globals.css

  config/
    routes.ts


4. 환경 변수 설정
Replit 환경 변수 (Secrets) 예시:
VITE_SUPABASE_URL


VITE_SUPABASE_ANON_KEY


(Admin 패널용 백엔드가 별도로 있다면) ADMIN_SERVICE_KEY 등은 서버 전용으로 보관


Vite 기준으로 import.meta.env.VITE_SUPABASE_URL 형태로 사용한다.

5. Supabase Client (lib/supabaseClient.ts)
// src/lib/supabaseClient.ts
import { createClient } from "@supabase/supabase-js";

const supabaseUrl = import.meta.env.VITE_SUPABASE_URL as string;
const supabaseAnonKey = import.meta.env.VITE_SUPABASE_ANON_KEY as string;

export const supabase = createClient(supabaseUrl, supabaseAnonKey);


6. Routing 구조 (react-router-dom)
App.tsx 예시:
// src/App.tsx
import { BrowserRouter, Routes, Route } from "react-router-dom";
import { LandingPage } from "./routes/influencer/LandingPage";
import { CampaignListPage } from "./routes/influencer/CampaignListPage";
import { CampaignDetailPage } from "./routes/influencer/CampaignDetailPage";
import { InfluencerDashboardPage } from "./routes/influencer/InfluencerDashboardPage";
import { ProfilePage } from "./routes/influencer/ProfilePage";

import { AdminDashboardPage } from "./routes/admin/AdminDashboardPage";
import { AdminCampaignListPage } from "./routes/admin/AdminCampaignListPage";
import { AdminCampaignDetailPage } from "./routes/admin/AdminCampaignDetailPage";

function App() {
  return (
    <BrowserRouter>
      <Routes>
        {/* Influencer */}
        <Route path="/" element={<LandingPage />} />
        <Route path="/campaigns" element={<CampaignListPage />} />
        <Route path="/campaigns/:id" element={<CampaignDetailPage />} />
        <Route path="/dashboard" element={<InfluencerDashboardPage />} />
        <Route path="/profile" element={<ProfilePage />} />

        {/* Admin */}
        <Route path="/admin" element={<AdminDashboardPage />} />
        <Route path="/admin/campaigns" element={<AdminCampaignListPage />} />
        <Route path="/admin/campaigns/:id" element={<AdminCampaignDetailPage />} />
      </Routes>
    </BrowserRouter>
  );
}

export default App;


7. Tailwind & shadcn/ui 설정
Tailwind 설정
tailwind.config.cjs 예시:
module.exports = {
  content: ["./index.html", "./src/**/*.{ts,tsx,js,jsx}"],
  theme: {
    extend: {
      colors: {
        brand: "#8a01ff",
      },
      borderRadius: {
        xl: "1rem",
      },
      boxShadow: {
        soft: "0 10px 25px rgba(0,0,0,0.06)",
      },
    },
  },
  plugins: [],
};

src/styles/globals.css 에 기본 Tailwind base/style 포함.

8. 주요 페이지 Skeleton
여기서는 각 페이지가 어떤 책임을 갖는지만 정의한다.
 상세 UI는 shadcn/ui Card, Button, Tabs 등을 사용해 구현할 수 있다.
8.1 Influencer – LandingPage
상단 Hero: “Apply for K-Beauty / Food / Lifestyle gifting campaigns”


버튼: “View Campaigns”


로그인 상태에 따라:


미로그인 → Google Login 버튼


로그인 + profile 미완료 → “Complete your profile” 안내


로그인 + profile 완료 → “Go to Dashboard”


8.2 Influencer – CampaignListPage
필터 (Beauty / Food / Lifestyle)


CampaignCard 리스트


Apply 버튼은 Part 1의 조건에 따라 enabled/disabled


8.3 Influencer – CampaignDetailPage
캠페인 상세 정보 표시


Notion 링크 (Full guidelines)


Apply 버튼 + Address 확인 팝업


8.4 Influencer – InfluencerDashboardPage
상단: Score, Penalty, Restricted 정보


Tabs: Pending / Approved / Shipping / Delivered / Uploaded / Deadline Missed / Rejected


각 탭에서 application 상태를 기반으로 목록 조회


Shipping issue report 버튼


8.5 Influencer – ProfilePage
모든 필수 필드 입력 폼


Save 시 profile_completed 상태 업데이트



9. Admin 페이지 Skeleton
9.1 AdminDashboardPage
Active Campaigns 수


Pending Applicants 수


Shipping Pending 수


Upload Pending 수


9.2 AdminCampaignListPage
campaigns 테이블 조회


Status filter, category filter, 정렬 옵션


각 캠페인 클릭 → AdminCampaignDetailPage로 이동


9.3 AdminCampaignDetailPage
내부 Tabs:
Overview


Applicants


Shipping


Uploads


Notes


각 탭에서 Part 2, Part 3, Part 4, Part 5에 정의한 스키마/상태를 기반으로 UI 구성.

10. Supabase 연동 패턴
10.1 데이터 Fetch
간단한 예시 (Influencer 캠페인 리스트):
// src/hooks/useCampaigns.ts
import { useEffect, useState } from "react";
import { supabase } from "../lib/supabaseClient";

export function useCampaigns(category?: string) {
  const [campaigns, setCampaigns] = useState<any[]>([]);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    async function load() {
      setLoading(true);
      let query = supabase
        .from("campaigns")
        .select("*")
        .eq("status", "active");

      if (category && category !== "all") {
        query = query.eq("category", category);
      }

      const { data, error } = await query;
      if (!error && data) setCampaigns(data);
      setLoading(false);
    }

    load();
  }, [category]);

  return { campaigns, loading };
}

이 패턴을 admin/influencer 전반에 걸쳐 재사용하면 된다.

11. Replit 상에서의 배포 고려사항
Vite dev server 포트가 Replit에서 자동 연결되도록 설정


Supabase 환경 변수는 Replit Secrets에 설정


Admin 경로(/admin/*)는 별도의 Guard를 통해 auth role 체크


Notification 트리거는 서버 사이드에서 Supabase RPC 또는 별도 backend route로 처리 가능



12. Replit용 최종 프롬프트 문장 예시
아래 텍스트를 하나의 프롬프트로 Replit AI에 전달할 수 있다.
 (한국어 설명 + 영어 요구사항 혼합 가능하지만, 개발 지시 자체는 영어로 두는 것이 안전하다.)
예시:
Project name: Collaboom MVP
Please build a React + TypeScript + Vite + Tailwind + shadcn/ui + Supabase application following this PRD:
Influencer side:


Google OAuth login via Supabase


Profile completion gating (cannot apply unless profile_completed = true)


Campaign browsing with filters (Beauty / Food / Lifestyle)


Campaign detail + apply flow, creating rows in applications


Influencer dashboard with tabs: Pending / Approved / Shipping / Delivered / Uploaded / Deadline Missed / Rejected


Score and penalty display (read-only from influencers.score, influencers.penalty)


Shipping issue report button per application


Admin side:


Admin login (Supabase email/password)


Campaign CRUD (using campaigns table)


Applicants tab, with approve/reject actions and bulk approve support


Shipping tab, where CSV can be uploaded and mapped to shipping rows


Uploads tab, where admin can mark uploaded/missed and trigger score/penalty updates


Notes tab, where admin_notes are stored per influencer/application


Database:


Use the following Supabase schema:


influencers, campaigns, applications, shipping, uploads, score_events, penalty_events, admin_notes, notifications


Follow the PRD for field names and relationships


Styling:


Use Tailwind + shadcn/ui


Primary brand color: #8a01ff


Layout should look modern and clean, mobile-first


Please create the folder structure exactly as described and implement basic working versions of each page and route. Focus on correctness of data flow and state transitions according to the PRD (score/penalty logic, application state transitions, campaign status updates).

